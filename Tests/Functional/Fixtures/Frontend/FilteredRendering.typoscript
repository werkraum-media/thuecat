@import 'EXT:thuecat/Tests/Functional/Fixtures/Frontend/PluginRendering.typoscript'

# The editor filter is the subject of the test, so it cannot live in the shared file.
plugin.tx_thuecat.settings.towns = 1
