@import 'EXT:thuecat/Tests/Functional/Fixtures/Frontend/PluginRendering.typoscript'

# Both plugins live on page 10; form + redirect must target it. Restated rather
# than inherited: the test fails misleadingly if the shared default ever moves.
plugin.tx_thuecat.settings.page.pid.thuecat_attraction_search = 10
plugin.tx_thuecat.settings.itemsPerPage = 10
